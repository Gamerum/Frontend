import { Item } from '../types/profileItem';

const profileItems: Item[] = [
  [
    {
      label: 'settings',
      icon: 'pi pi-cog ',
      path: '/settings',
    },
    {
      label: 'sign_out',
      icon: 'pi pi-sign-out',
      path: '/signout',
    },
  ],
];

export default profileItems;
